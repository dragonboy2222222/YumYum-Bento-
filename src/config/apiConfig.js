// src/config/apiConfig.js

/**
 * Configuration file for API endpoints.
 * * IMPORTANT: Replace the placeholder URL below with the actual
 * base URL of your PHP backend scripts (where checkout_process.php resides).
 * Ensure you use your exact domain and path.
 */
export const API_BASE_URL = 'http://localhost:3000/Webpage/api';